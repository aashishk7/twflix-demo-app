---

apiVersion: v1
kind: Service
metadata:
  name: netflix-demo-db
  labels:
    app: netflix-demo-db
    release: primary
spec:
  type: ClusterIP
  ports:
  - name: mongodb
    port: 27017
    targetPort: mongodb
  selector:
    app: netflix-demo-db
    release: primary
