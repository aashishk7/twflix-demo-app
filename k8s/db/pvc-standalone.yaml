---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: demo-app-db
    release: primary
  name: demo-app-db
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
