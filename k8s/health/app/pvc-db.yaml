---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: netflix-demo-db
    release: primary
  name: netflix-demo-db
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
