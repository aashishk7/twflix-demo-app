---

apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: netflix-demo
spec:
  hosts:
  - netflix-demo.acme.com
  - netflix-demo
  gateways:
  - netflix-demo
  http:
  - route:
    - destination:
        host: netflix-demo
        subset: primary
        port:
          number: 80

---

apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: netflix-demo
spec:
  host: netflix-demo
  subsets:
  - name: primary
    labels:
      release: primary

---

apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: netflix-demo
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - netflix-demo.acme.com
