---

apiVersion: v1
kind: Service
metadata:
  name: netflix-demo
  labels:
    app: netflix-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: netflix-demo
