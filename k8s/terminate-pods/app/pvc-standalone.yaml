---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: twflix-microservice-db
    release: primary
  name: twflix-microservice-db
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
